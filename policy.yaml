jurisdictions: ["US-FL"]

tiers:
  C:
    requires: ["identity", "delivery", "timeAnchor"]
  B:
    requires: ["identity", "notary|witness", "possession", "timeAnchor"]
  A:
    requires: ["identity", "recording", "taxPaid", "timeAnchor"]

money:
  stablecoin: "ADA"

roles:
  trust_key: "<enter trust stake key>"
  oracle_pk: "<oracle public key>"
